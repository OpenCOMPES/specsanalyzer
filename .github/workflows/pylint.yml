name: python

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: actions/checkout@v3
      with:
        lfs: true
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        git lfs pull
        python -m pip install --upgrade pip
        pip install pycodestyle pylint mypy pytest types-PyYAML
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Install package
      run: |
        python -m pip install .
    - name: Test with pytest
      continue-on-error: true
      run: |
        python -m pytest -sv tests
    - name: pycodestyle
      continue-on-error: true
      run: |
        python -m pycodestyle --ignore=E501 specsanalyzer tests 
    - name: pylint
      continue-on-error: true
      run: |
        python -m pylint specsanalyzer tests 
    - name: mypy
      run: |
        python -m mypy --ignore-missing-imports --follow-imports=silent --no-strict-optional specsanalyzer tests